# Telegram-Bot

A tiny Telegram bot (Python). Minimal, ready to extend.

Quick start
- Create a bot token via @BotFather
- Copy `.env.example` → `.env` and set `BOT_TOKEN`
- Run: `python main.py`

Notes
- Lightweight single-file entrypoint; add handlers in `handlers/` or similar.
